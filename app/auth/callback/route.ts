import { type NextRequest, NextResponse } from "next/server";
import { cookies } from "next/headers";
import { createClient, isSupabaseConfigured } from "@/lib/supabase/server";
import { safeInternalPath } from "@/lib/auth/redirects";
import { ACTIVE_ORGANIZATION_COOKIE } from "@/lib/auth/context";
export async function GET(request:NextRequest){if(!isSupabaseConfigured())return NextResponse.redirect(new URL("/login?error=Supabase%20environment%20variables%20are%20not%20configured.",request.url));const url=new URL(request.url);const code=url.searchParams.get("code");const invite=url.searchParams.get("flow")==="invite";const authError=url.searchParams.get("error_code")||url.searchParams.get("error");if(authError)return NextResponse.redirect(new URL("/login?error=Invitation%20link%20expired%20or%20is%20invalid.",request.url));const next=safeInternalPath(url.searchParams.get("next"));if(code){const supabase=await createClient();if(invite)await supabase.auth.signOut({scope:"local"});const {error}=await supabase.auth.exchangeCodeForSession(code);if(error)return NextResponse.redirect(new URL("/login?error=Invitation%20link%20expired%20or%20is%20invalid.",request.url));const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.redirect(new URL("/login?error=The%20invitation%20could%20not%20establish%20a%20session.",request.url));if(invite)(await cookies()).delete(ACTIVE_ORGANIZATION_COOKIE);}return NextResponse.redirect(new URL(next,request.url));}
