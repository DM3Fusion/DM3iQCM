import Link from "next/link";
import { Badge,PageHeader } from "@/components/ui";
import { getLiveOrganizationData } from "@/lib/data/case-repository";
import { NavigableRow } from "@/components/navigable-row";
import { formatPhone } from "@/lib/format-phone";
export const metadata={title:"Customers"};
export default async function Page({searchParams}:{searchParams:Promise<{message?:string}>}){const [data,query]=await Promise.all([getLiveOrganizationData(),searchParams]);return <><PageHeader eyebrow="Relationships" title="Customers" description="Live customer records for the active organization." action={<Link className="primary-button" href="/customers/new">＋ New Customer</Link>}/>{query.message?<div className="success-alert page-notice">{query.message}</div>:null}<section className="panel">{data.customers.length?<div className="table-scroll"><table><thead><tr><th>Customer Number</th><th>Name</th><th>Type</th><th>Email</th><th>Phone</th><th>Status</th><th>Open Cases</th></tr></thead><tbody>{data.customers.map(customer=><NavigableRow key={customer.id} href={`/customers/${customer.id}`} label={`Open customer ${customer.customer_number}`}><td><b className="case-link">{customer.customer_number}</b></td><td><b>{customer.name}</b></td><td>{customer.type}</td><td>{customer.email??"—"}</td><td>{formatPhone(customer.phone)}</td><td><Badge value={customer.status}/></td><td>{data.cases.filter(item=>item.customer_id===customer.id&&!['COMPLETED','CLOSED','CANCELLED'].includes(item.status)).length}</td></NavigableRow>)}</tbody></table></div>:<div className="empty compact-empty"><h2>No customers yet</h2><p>Create the first customer before opening case work.</p><Link className="primary-button" href="/customers/new">Create Customer</Link></div>}</section></>}
