import Link from "next/link";
import { Badge } from "@/components/ui";
import { NavigableRow } from "@/components/navigable-row";
import { getLiveOrganizationData, displayName } from "@/lib/data/case-repository";
import { getServiceRequestMetrics } from "@/lib/live-dashboard-metrics";
export const metadata={title:"Service Desk"};
export default async function Page(){const data=await getLiveOrganizationData();const metrics=getServiceRequestMetrics(data.serviceRequests);return <><div className="page-header"><div><span className="eyebrow">Customer Service</span><h1>Service Desk</h1><p>Customer Service Dashboard</p></div><Link className="primary-button" href="/service-desk/new">＋ New Service Request</Link></div><div className="metric-grid service-desk-metrics">{metrics.map(metric=><Link key={metric.label} href={metric.href} className={`metric tone-${metric.tone}`}><span>{metric.label}</span><strong>{metric.value}</strong></Link>)}</div><section className="panel"><div className="section-head"><div><h2>Recent Service Requests</h2><p>Latest operational activity</p></div><Link href="/service-desk/requests">View All Service Requests →</Link></div>{data.serviceRequests.length?<div className="table-scroll"><table><thead><tr><th>Request</th><th>Customer</th><th>Subject</th><th>Priority</th><th>Status</th><th>Assigned</th><th>Updated</th></tr></thead><tbody>{data.serviceRequests.slice(0,8).map(request=><NavigableRow key={request.id} href={`/service-desk/${request.id}`} label={`Open service request ${request.request_number}`}><td><b className="case-link">{request.request_number}</b></td><td>{request.customer?.name??"—"}</td><td><b>{request.subject}</b></td><td><Badge value={request.priority}/></td><td><Badge value={request.status}/></td><td>{displayName(request.assigned)}</td><td>{new Date(request.updated_at).toLocaleDateString()}</td></NavigableRow>)}</tbody></table></div>:<div className="empty compact-empty"><h2>No service requests yet</h2><p>Create the first service request to begin tracking customer support work.</p><Link className="primary-button" href="/service-desk/new">New Service Request</Link></div>}</section></>}
