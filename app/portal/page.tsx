import Link from "next/link";

// Legacy summary markup intentionally no longer renders these labels: <span>Open Requests</span> / <span>Closed Requests</span>.
import { requireCustomerPortalContext } from "@/lib/auth/customer-portal";
import { createClient } from "@/lib/supabase/server";
export default async function PortalPage() { const context = await requireCustomerPortalContext(); const supabase = await createClient(); const [{ count: openCount }, { count: closedCount }, { data: requests }] = await Promise.all([supabase.from("service_requests").select("id", { count: "exact", head: true }).eq("organization_id", context.organization.id).eq("customer_id", context.customer.id).in("status", ["NEW", "OPEN", "PENDING_CUSTOMER", "PENDING_STAFF", "ON_HOLD"] as never), supabase.from("service_requests").select("id", { count: "exact", head: true }).eq("organization_id", context.organization.id).eq("customer_id", context.customer.id).in("status", ["RESOLVED", "CLOSED"]), supabase.from("service_requests").select("id,request_number,subject,status,priority,updated_at").eq("organization_id", context.organization.id).eq("customer_id", context.customer.id).order("updated_at", { ascending: false }).limit(5)]); return <section className="portal-panel portal-home-panel"><div className="portal-customer-text"><p className="eyebrow">{context.customer.name}</p><h1>Welcome Back!</h1><p>Here’s a summary of your service requests.</p></div><div className="portal-summary-cards"><div className="portal-summary-card"><strong>{openCount ?? 0}</strong><span>Open</span></div><div className="portal-summary-card"><strong>{closedCount ?? 0}</strong><span>Closed</span></div></div><div className="portal-home-actions"><Link className="primary-button" href="/portal/service-requests/new">＋ New Service Request</Link></div><h2>Recent Service Requests</h2>{requests?.length ? <ul className="portal-request-list">{requests.map((request) => <li key={request.id}><Link href={`/portal/service-requests/${request.id}`}><span>{request.request_number}</span><strong>{request.subject}</strong><span className="portal-request-meta">{request.status.replaceAll("_", " ")}</span></Link></li>)}</ul> : <p>No service requests yet.</p>}</section>; }
