/* eslint-disable @typescript-eslint/no-explicit-any */
import { PageHeader } from "@/components/ui";
import Link from "next/link";
import { saveOrganizationSettings } from "@/lib/data/organization-administration-actions";
import { createClient } from "@/lib/supabase/server";
import { getAccessContext } from "@/lib/auth/context";

export default async function Page(){const access=await getAccessContext();const db=await createClient();const id=access?.activeOrganization?.id;const [{data:settings},{data:organization}]=id?await Promise.all([(db as any).from("organization_settings").select("timezone,timezone_source,timezone_resolved_from_postal_code,default_priority").eq("organization_id",id).maybeSingle(),(db as any).from("organizations").select("business_postal_code").eq("id",id).maybeSingle()]):[{data:null},{data:null}];const timezoneSource=settings?.timezone_source??"DEFAULT";return <><PageHeader eyebrow="Administration" title="Organization Defaults" description="Set safe organization-wide operational defaults."/><section className="panel form-panel"><form action={saveOrganizationSettings} className="entity-form"><input type="hidden" name="portalEnabled" value="true"/><input type="hidden" name="portalSubmissionEnabled" value="true"/><input type="hidden" name="portalShowPriority" value="true"/><label><span>Business ZIP / Postal Code</span><input name="businessPostalCode" defaultValue={organization?.business_postal_code??""} placeholder="12345"/></label><label><span>Time Zone</span><select name="timezoneMode" defaultValue={timezoneSource==="MANUAL"?"MANUAL":"ZIP"}><option value="ZIP">Use ZIP-derived timezone</option><option value="MANUAL">Manual override</option></select></label><label><span>Time Zone Override</span><input name="timezone" defaultValue={settings?.timezone??"UTC"} placeholder="America/New_York"/></label><p className="muted">Resolved time zone: {settings?.timezone_resolved_from_postal_code??settings?.timezone??"UTC"}</p><label><span>Default Case Priority</span><select name="defaultPriority" defaultValue={settings?.default_priority??"NORMAL"}><option>NORMAL</option><option>LOW</option><option>HIGH</option><option>URGENT</option></select></label><div className="form-actions"><Link href="/administration">Cancel</Link><button className="primary-button">Save Changes</button></div></form></section></>}
