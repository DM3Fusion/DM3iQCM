import Link from "next/link";import { PageHeader,Badge } from "@/components/ui";import { enterOrganizationWorkspaceAction,provisionMemberAction,updateMembershipAction,updateOrganizationAction } from "@/lib/data/platform-actions";import { getOrganizationAdministration } from "@/lib/data/platform-repository";
const roles=["BUSINESS_OWNER","BUSINESS_ADMIN","STAFF_MANAGER","STAFF_USER"] as const;
export default async function Page({params,searchParams}:{params:Promise<{organizationId:string}>;searchParams:Promise<{message?:string;error?:string}>}){const [{organizationId},query]=await Promise.all([params,searchParams]);const {organization,members}=await getOrganizationAdministration(organizationId);return <><PageHeader eyebrow="Platform Administration" title={organization.name} description={`Organization administration · ${organization.slug}`} action={organization.status==="ACTIVE"?<form action={enterOrganizationWorkspaceAction}><input type="hidden" name="organizationId" value={organization.id}/><button className="primary-button">Enter Workspace →</button></form>:undefined}/>{query.message?<div className="success-alert page-notice">{query.message}</div>:null}{query.error?<div className="form-alert page-notice">{query.error}</div>:null}<div className="admin-detail-grid"><section className="panel detail-section"><div className="section-head"><div><h2>Organization details</h2><p>ID: {organization.id}</p></div><Badge value={organization.status}/></div><form action={updateOrganizationAction} className="entity-form"><input type="hidden" name="organizationId" value={organization.id}/><div className="form-grid"><label><span>Name</span><input name="name" defaultValue={organization.name} required/></label><label><span>Slug</span><input name="slug" defaultValue={organization.slug} required/></label><label><span>Status</span><select name="status" defaultValue={organization.status}><option value="ACTIVE">Active</option><option value="SUSPENDED">Suspended</option><option value="ARCHIVED">Archived</option></select></label><label><span>Created</span><input value={new Date(organization.created_at).toLocaleString()} disabled/></label></div><div className="form-actions"><button className="primary-button">Save changes</button></div></form></section><aside className="panel detail-section admin-summary"><h2>Live summary</h2><dl><div><dt>Active users</dt><dd>{organization.activeUsers}</dd></div><div><dt>Business owners</dt><dd>{organization.businessOwners} / 2</dd></div><div><dt>Business admins</dt><dd>{organization.businessAdmins} / 2</dd></div><div><dt>Customers</dt><dd>{organization.customers}</dd></div><div><dt>Open cases</dt><dd>{organization.openCases}</dd></div></dl></aside></div><section className="panel"><div className="section-head"><div><h2>Users / Access</h2><p>Internal organization memberships. Public portal access is managed separately.</p></div></div><details className="admin-provision"><summary>＋ Add User</summary><form action={provisionMemberAction} className="mini-form"><input type="hidden" name="organizationId" value={organization.id}/><label><span>Registered user email</span><input type="email" name="email" required/></label><label><span>Organization role</span><select name="role">{roles.map(role=><option key={role} value={role}>{role.replaceAll("_"," ")}</option>)}</select></label><div className="mini-actions"><button>Provision User</button></div></form><p>Users must already have a DM3iQ profile. Auth invitations are not configured in this milestone.</p></details>{members.length?<div className="table-scroll"><table><thead><tr><th>User</th><th>Role</th><th>Status</th><th>Added</th><th>Change access</th></tr></thead><tbody>{members.map(m=><tr key={m.id}><td><b>{m.profile.display_name||m.profile.email||"Unnamed user"}</b><small className="table-secondary">{m.profile.email}</small></td><td>{m.role.replaceAll("_"," ")}</td><td><Badge value={m.is_active?"ACTIVE":"INACTIVE"}/></td><td>{new Date(m.joined_at).toLocaleDateString()}</td><td><form action={updateMembershipAction} className="membership-form"><input type="hidden" name="organizationId" value={organization.id}/><input type="hidden" name="membershipId" value={m.id}/><select name="role" defaultValue={m.role}>{roles.map(role=><option key={role} value={role}>{role.replaceAll("_"," ")}</option>)}</select><select name="active" defaultValue={String(m.is_active)}><option value="true">Active</option><option value="false">Inactive</option></select><button>Save</button></form></td></tr>)}</tbody></table></div>:<div className="no-results">No organization users have been provisioned.</div>}</section><Link className="auth-link" href="/admin/organizations">← All organizations</Link></>}
