import Link from "next/link";import { PageHeader } from "@/components/ui";import { requireSuperAdmin } from "@/lib/auth/context";import { createOrganizationAction } from "@/lib/data/platform-actions";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){await requireSuperAdmin();const params=await searchParams;return <><PageHeader eyebrow="Platform Administration" title="Create organization" description="Provision an active business workspace."/><section className="panel form-panel admin-form-panel">{params.error?<div className="form-alert">{params.error}</div>:null}<form action={createOrganizationAction} className="entity-form"><div className="form-grid"><label className="full"><span>Organization name</span><input name="name" required maxLength={160}/></label><label className="full"><span>Slug</span><input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" placeholder="business-name"/><small>Lowercase letters, numbers, and hyphens.</small></label></div><div className="form-actions"><Link href="/admin/organizations">Cancel</Link><button className="primary-button">Create Organization</button></div></form></section></>}
