import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import test from "node:test";
test("administration cards are full-surface links without nested anchors",()=>{const page=readFileSync("app/administration/page.tsx","utf8");const css=readFileSync("app/globals.css","utf8");assert.match(page,/className=\"panel admin-config-card\"/);assert.match(page,/href=\{`\/administration\/\$\{slug\}`\}/);assert.match(page,/className=\"admin-card-action\"/);assert.doesNotMatch(page,/admin-config-card[^>]*>[\s\S]*<Link/);assert.match(css,/admin-config-card:focus-visible/);assert.match(css,/cursor:pointer/);});
test("organization defaults persist timezone settings and return to their form",()=>{const action=readFileSync("lib/data/organization-administration-actions.ts","utf8");const page=readFileSync("app/administration/defaults/page.tsx","utf8");assert.match(action,/business_postal_code/);assert.match(action,/timezone_source/);assert.match(action,/administration\/defaults\?message=Changes%20Saved/);assert.doesNotMatch(action,/administration\/service-desk\?message=Changes%20Saved/);assert.match(page,/Business ZIP \/ Postal Code/);assert.match(page,/timezoneMode/);assert.match(page,/Time Zone Override/);});
